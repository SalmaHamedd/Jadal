/// A lightweight publisher/author option for the blog filter dialog —
/// `GET /blog/authors`.
class BlogAuthorOption {
  final int id;
  final String name;
  const BlogAuthorOption({required this.id, required this.name});
}
